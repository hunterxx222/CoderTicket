  <% @events.in_groups_of(4) do |g| %>
    <div class="row">
      <% g.compact.each do |e| %>
          <%= render partial: 'card', locals: {event: e} %>
      <% end %>
    </div>
    <%= link_to "Edit Event", edit_event_path(g.compact), class: "btn btn-primary" %>
  <% end %>
